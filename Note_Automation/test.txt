pip install pytest-tui pytest 插件查看用例运行总数和情况

安装软件包指定版本 selenium 命令：pip install selenium==4.9.0

安装软件包指定版本 appium-python-client 命令：pip install appium-python-client==2.9.0

pip install pytest==7.4.4

安装 pytest 插件 pip install pytest-rerunfailures

安装 allure-pytest插件 pip install allure-pytest  2.12.0

安装cv2库 pip install opencv-python

安装scikit pip install scikit-image

安装pypdf2 pip install PyPDF2

Appium-Python-Client 2.9.0

appium 1.22.3

# 记录不同级别的日志
# logging.debug('这是一个调试信息')
# logging.info('这是一个普通信息')
# logging.warning('这是一个警告信息')
# logging.error('这是一个错误信息')
# logging.critical('这是一个严重错误信息')

# assert element.exists(),'按钮不存在' #断言按钮是否存在
# assert element.is_displayed(),'按钮不可见' #断言按钮是否可见
# assert element.is_enabled(),'按钮不可点击' #断言按钮是否可以点击
# 添加元素可点击性判断

# activity_before_click = self.driver.current_activity
#
# activity_after_click = self.driver.current_activity
#
# if activity_before_click == activity_after_click:
#     assert False, f"页面未成功跳转"
# else:
#     print("页面已成功跳转")

@allure.feature(‘功能名称’)：相当于 testsuite
@allure.story(’子功能名称‘)：对应这个功能或者模块下的不同场景，相当于 testcase
@allure.step('步骤')：测试过程中的每个步骤，放在具体逻辑方法中
with allure.step：可以放在测试用例方法里面
@allure.attach('具体文本信息')
附加信息：数据，文本，图片，视频，网页


@allure.severity("normal")
blocker：阻塞缺陷（功能未实现，无法下一步）
critical：严重缺陷（功能点缺失）
normal： 一般缺陷（边界情况，格式错误）
minor：次要缺陷（界面错误与ui需求不符）
trivial： 轻微缺陷（必须项无提示，或者提示不规范）