# 笔记自动化测试 — 使用指南

> 本文档面向**无编程经验的测试人员**，用最通俗的语言解释：怎么写测试用例、怎么定义页面元素、项目文件是干什么的。
> **规则：每个章节顶部先给示例，再讲规则。**

---

## 一、两分钟快速理解

这个项目做一件事：**把 Excel 里的测试步骤，自动在设备上执行**。

```
Excel 测试用例（你写的）          自动化程序（自动跑的）
─────────────────────────       ────────────────────────
操作步骤（J列）：                 →  打开笔记应用
1.点击【笔记】                    →  点击"笔记"图标
2.点击【创建笔记】                 →  点击创建按钮
3.点击【手写笔记】                 →  选择手写笔记

预期结果（K列）：                 →  每步执行后自动比对页面
步骤2：【笔记首页引导页】          →  对比实际页面XML vs 预期XML
步骤3：【创建菜单弹窗】            →  检查菜单元素是否全部存在
```

**你需要维护两个 Excel 文件：**

| 文件 | 干什么用 | 类比 |
|---|---|---|
| `test_cases.xlsx` | 写测试用例：先做什么、再做什么 | 就像"测试操作手册" |
| `elements.xlsx` | 告诉程序每个按钮/文字怎么找到 | 就像"页面元素通讯录" |

---

## 二、测试用例怎么写（test_cases.xlsx）

### 示例（先看这个）

```
优先级：P0
标题：未登录-创建手写笔记
所属模块：笔记首页
前置条件：
    1. 新设备或重置设备后未打开过【笔记】
    2. 没有登录过账号
    3.【海外设备执行】

操作步骤：
    1.设备桌面点击【笔记】应用
    2.点击【创建笔记】笔记按钮
    3.点击创建菜单中【手写笔记】
    4.创建笔记页面点击【创建】按钮
    5.点击【退出手写笔记】

预期结果：
    步骤2：【笔记首页引导页】
    步骤3：【创建菜单弹窗】
    步骤4：【手写笔记创建页】
    步骤5：【笔记列表页】
```

**重要变更：J 列只管操作，K 列只管断言，两者分离。** J 列不再写"检查XXX"，所有验证统一放在 K 列的预期结果中。

### 列说明

Excel 中以下列会被程序读取：

| 列 | 列名 | 说明 | 可以不填？ |
|---|---|---|---|
| G | 标题 | 用例名称，简短描述要测什么 | **必填** |
| H | 优先级 | P0（核心）/ P1（重要）/ P2（次要） | 建议填写 |
| D | 所属模块 | 用例属于哪个页面，如"笔记首页" | 建议填写 |
| I | 前置条件 | 在什么设备上执行、需要什么环境 | 可为空 |
| J | 操作步骤 | 一步步怎么做，用【】标记要操作的按钮 | **必填** |
| K | 预期结果 | 步骤执行后自动比对页面XML，格式见下方 | 建议填写 |

其他列（A 维护人、B 用例类型、C 编号等）程序不读取，可自由填写。

### 操作步骤写法

**核心规则：用 `【】` 把要操作的按钮/文字括起来，程序会自动找到它。**

每一步前面加数字编号（1. 或 1、都可以），换行分隔。

```
1.点击【笔记】                      → 程序：找到"笔记"，点击它
2.检查【笔记首页引导UI】             → 程序：检查首页所有引导元素是否存在
3.点击创建菜单中【手写笔记】         → 程序：找到"手写笔记"，点击它
4.输入【笔记标题】输入test          → 程序：在标题输入框里输入"test"
5.长按【笔记条目】                  → 程序：长按笔记条目
```

### 动作关键词

程序通过**动作关键词**判断要做什么：

| 写在步骤里的词 | 程序做的事 | 是否需要 `【】` | 示例 |
|---|---|---|---|
| `点击` `打开` `进入` `选择` `双击` `退出` `返回` `清空` `确认` `关闭` | 点击元素 | 是 | `点击【创建笔记】` |
| `输入` | 在输入框里填内容 | 是 | `输入【笔记标题】测试名称` |
| `长按` | 长按元素 | 是 | `长按【笔记条目】` |
| `点击坐标` | 按比例坐标点击 | 是 | `点击坐标【屏幕中央】` |
| `长按坐标` | 按比例坐标长按 | 是 | `长按坐标【左上角】` |
| `滑动` | 按比例坐标滑动 | 是 | `滑动【左滑关闭】` |
| `上滑` `向上滑动` `下滑` `向下滑动` `左滑` `向左滑动` `右滑` `向右滑动` | 全屏方向滑动 | **否** | `向下滑动` |
| `按返回键` `返回键` | 按系统返回键 | **否** | `按返回键` |

> **注意：** `检查`/`查看`/`校验` 开头的步骤会被跳过（由 K 列预期结果检查）。**方向滑动和返回键是硬编码设备操作**，直接写关键词即可，不要加 `【】`。

**规则：** 如果写了 `【xxx】` 但没写动作词，默认当"点击"处理。如果什么都没写，这一步跳过。

### 输入动作的特殊规则

输入动作的步骤文本中可以写第二个 `【】` 来引用前置条件中定义的变量：

```
前置条件：【输入用户名-testuser】

操作步骤：
1.输入【用户名输入框】【用户名】    → 程序会把 "testuser" 填入输入框
```

如果步骤文本中直接写了"输入XXXX字符"，程序会提取 X 作为输入内容：
```
1.输入【笔记标题】输入test字符      → 程序填入 "test"
```

### 前置条件写法

前置条件放在 `【】` 中，支持以下几种：

**条件型（根据设备自动判断是否执行）：**

| 写法 | 含义 | 例子 |
|---|---|---|
| `【国内设备执行】` | 仅国内设备跑这条用例 | 你的设备是海外版 → **自动跳过** |
| `【海外设备执行】` | 仅海外/全球设备跑 | 你的设备是国内版 → **自动跳过** |
| `【平板设备执行】` | 仅平板设备跑 | 阅读器 → **自动跳过** |
| `【阅读器设备执行】` | 仅阅读器设备跑 | 平板 → **自动跳过** |
| `【黑白设备执行】` | 仅黑白屏设备跑 | 彩屏 → **自动跳过** |
| `【彩色设备执行】` | 仅彩屏设备跑 | 黑白屏 → **自动跳过** |

**版本型（根据固件版本判断）：**

| 写法 | 含义 |
|---|---|
| `【4.2-版本执行】` | 设备固件版本 ≥ 4.2 才执行 |

**变量型（定义输入值）：**

| 写法 | 含义 |
|---|---|
| `【输入用户名-testuser】` | 定义一个叫"用户名"的变量，值是"testuser" |

**多条件可同时写，换行分隔：**
```
【平板设备执行】
【4.2-版本执行】
【输入笔记名-测试笔记】
```

### 预期结果写法（K 列）

K 列用于**页面级断言**：每步执行后自动获取当前屏幕的页面结构，和预期 XML 对比，检查关键元素是否全部存在。

**格式：**

```
步骤2：【笔记首页引导页】
步骤4：【创建菜单弹窗】
步骤10：【笔记列表页】
```

- 每行格式：`步骤{N}：【{预期结果key}】`
- 不含 `【】` 的行视为注释，跳过
- K 列为空也可以，程序会提示 WARNING 但不阻塞执行

**预期结果 key 在元素表「预期结果」sheet 中定义**（5 列：元素标识 / 匹配文本 / 页面XML / 检查元素 / 用途说明），详见本文档第 3.1 节。

**对比逻辑（C 列 XML / D 列 XPath 二选一）：**
- D 列（检查元素）：逐条 `find_element` 找 XPath，可选「,期望文本」对比元素文字
- C 列（页面XML）：只比较 `class` + `resource-id`，text 差异仅 INFO
- 预期有的元素/文本不匹配 → **用例失败**

### 模块筛选

通过环境变量 `NOTE_TEST_MODULES` 只跑指定模块的用例：

```bash
# 只跑"笔记首页"和"手写笔记"模块
NOTE_TEST_MODULES=笔记首页,手写笔记 pytest boox_automation/tests/test_excel_runner.py -s

# 不设环境变量 = 全跑（默认行为）
pytest boox_automation/tests/test_excel_runner.py -s
```

模块名对应 Excel 用例表的 D 列（所属模块），必须完全一致。

### 优先级筛选

程序默认只跑 P0 用例。如需跑其他优先级，修改 `test_excel_runner.py` 文件第 44 行：

```python
PRIORITY_FILTER = "P0"   # 改成 "" 跑全部，改成 "P1" 跑 P1+P2
```

| 设置值 | 跑的用例 |
|---|---|
| `"P0"` | 仅 P0 |
| `"P1"` | P1 + P2 |
| `"P2"` | 仅 P2 |
| `""` | 全部 |

---

## 三、元素怎么定义（elements.xlsx）

### 示例（先看这个）

一个完整的元素定义（表格中一行）：

```
key:        笔记首页.创建笔记按钮
match:      创建笔记
locator:    //android.widget.TextView[@resource-id="com.onyx.android.note:id/tv_create"]
action:     click
operation:  点击首页的创建笔记按钮，弹出创建类型选择菜单
index:      1
```

有了这条定义，当测试步骤中写 `点击【创建笔记】` 时，程序就能找到这个按钮并点击。

### Excel 结构

`elements.xlsx` 是一个 Excel 文件，包含多个 **Sheet（工作表）**。

- **每个 Sheet = 一个页面**。比如"笔记首页"的所有元素放在 `note_home` sheet 中。
- Sheet 名称可以随便取，但建议用英文，如 `note_home`、`note_handwritten`。
- **`预期结果` sheet**：专门存放页面预期 XML，供 K 列断言使用（见下方 3.1 节）。

当前已有的 Sheet：

| Sheet 名 | 对应的页面 |
|---|---|
| `note_home` | 笔记首页（创建按钮、笔记列表、引导弹窗等） |
| `note_handwritten` | 手写笔记编辑页（退出按钮、工具栏等） |
| `note_text` | 文本笔记编辑页 |
| `note_infinite` | 无边笔记编辑页 |
| `note_meeting` | 会议笔记编辑页 |
| `system` | 系统级通用元素（Dock 栏、存储入口等） |

### 列说明

| 列 | 列名 | 说明 | 必填？ | 示例值 |
|---|---|---|---|---|
| A | `key` | 元素唯一 ID，格式：`页面.元素名` | **必填** | `笔记首页.创建笔记按钮` |
| B | `match` | 匹配文本，步骤中 `【】` 里的字 | **必填** | `创建笔记` |
| C | `locator` | 定位方式：XPath / 纯文本 / 坐标 `x,y`。支持多设备「键：」分块 | **必填** | `//*[@resource-id="..."]` |
| D | `action` | 动作类型（中文），不填自动推断 | 建议填写 | `点击` |
| E | `operation` | 这个元素是干什么的（日志用） | 建议填写 | `点击首页的创建按钮` |
| F | `index` | 同类元素的序号，从 0 开始 | 选填 | `1` |

> **已移除的列：** `关闭方式`、`设备适配`、`多项检查`、`dismiss_with`、`overrides`、`checks` — 弹窗验证统一由 K 列预期结果完成，多设备适配统一在 `locator` 列通过「键：」分块完成。

### match 列 — 匹配文本

**这个是最重要的列。** 程序通过它把步骤中的 `【xxx】` 和元素关联起来。

```
步骤中写：【创建笔记】
match 列的值：创建笔记        → 匹配成功！
```

**规则：**
- match 的值要和步骤中 `【】` 里的文字**完全一致**
- 如果多个元素的 match 相同，程序会根据"所属模块"列自动选择最匹配的
- 如果找不到匹配，会打印警告，这一步会被跳过

### locator 列 — 定位方式

支持两种写法：

**写法一：直接写界面上的文字（推荐，最简单）**
```
locator: 创建笔记
```
程序会自动在页面上搜索包含"创建笔记"文字的元素。

**写法二：写 XPath 表达式（精确）**
```
locator: //android.widget.TextView[@resource-id="com.onyx.android.note:id/tv_create"]
```
XPath 可以通过 Appium Inspector 工具获取。

### action 列 — 动作类型

如果不填，程序会根据元素 key 中的关键词自动推断。手动填写可以覆盖自动推断（**推荐填中文值**）。

| 值 | 含义 | locator 格式 | 用在什么场景 |
|---|---|---|---|
| `点击` | 点击元素（**默认值**） | XPath | 按钮、菜单项、图标 |
| `输入` | 输入框 | XPath | 需要输入文字的地方 |
| `长按` | 长按元素 | XPath | 需要长按触发菜单的元素 |
| `校验toast` | 检查 Toast 提示 | XPath | 验证弹出的 Toast 信息 |
| `点击坐标` | 按比例坐标点击 | `x,y` 如 `0.5,0.3` | 点击没有固定 XPath 的位置（如屏幕中央） |
| `长按坐标` | 按比例坐标长按 | `x,y` 如 `0.5,0.3` | 长按没有固定 XPath 的位置 |
| `滑动` | 按比例坐标滑动 | `x1,y1,x2,y2` 如 `0.7,0.5,0.3,0.5` | 自定义起始终点滑动 |

> **注意：** `assert`/`assert_not`/`assert_text`/`dismiss` 已废弃。页面级验证改为 K 列预期结果。方向滑动（上滑/下滑/左滑/右滑）和返回键是设备级硬编码操作，**不在元素表中定义**，步骤中直接写关键词即可。

### index 列 — 元素序号

当页面有多个相同文字的元素时，用 index 指定第几个（从 0 开始）。

```
locator: //*[@resource-id="com.onyx.android.note:id/tv_title"]
index: 2          → 取第 3 个匹配的元素
```

### 预期结果 Sheet — 页面验证

`elements.xlsx` 中的 **`预期结果`** sheet 专门存放页面预期 XML，供 K 列断言使用。

**5 列结构**（C/D 二选一即可）：

| 列 | 中文名 | 说明 | 必填 |
|---|---|---|---|
| A | 元素标识 | 唯一 key，格式 `页面.页面状态`，如 `笔记首页.引导页` | ✓ |
| B | 匹配文本 | 用例 K 列 `【】` 中的文字，用于关联 | ✓ |
| C | 页面XML | 从 Appium Inspector 导出，支持多设备版本 | C/D 二选一 |
| D | 检查元素 | XPath 选择器，可选「,期望文本」对比元素文字 | C/D 二选一 |
| E | 用途说明 | 纯备注，人类可读描述 | |

**多设备 XML 格式（C 列）：**

默认 XML 放在最前面（所有设备兜底），设备专属 XML 用 `键：` 开头标注：

```
<hierarchy>
  ...默认 XML（所有设备兜底，必填）...
</hierarchy>

国内：
<hierarchy>
  ...国内版 XML（覆盖默认）...
</hierarchy>

海外：
<hierarchy>
  ...海外版 XML（覆盖默认）...
</hierarchy>

4.3：
<hierarchy>
  ...4.3+ 版本 XML（覆盖默认）...
</hierarchy>
```

支持的键：`国内` `海外` `全球` `平板` `阅读器` `6` `7.8` `10.3` `13.3` `4.2` `4.3` 等。

**如何获取 C 列的 XML：**

使用项目自带的提取脚本：

```bash
# 从设备获取当前页面 XML 并精简（需连接设备）
python boox_automation/scripts/extract_page_xml.py

# 从 Appium Inspector 导出的文件离线处理（无需设备）
python boox_automation/scripts/extract_page_xml.py --from-file ~/Desktop/page.xml

# 输出到文件
python boox_automation/scripts/extract_page_xml.py -o /tmp/page.xml
```

脚本会自动过滤无意义元素（纯容器、动态属性），输出精简 XML 直接粘贴到 C 列即可。

### D 列 — 检查元素

**格式**：每行一个 XPath，可选附加期望文本 `,期望文本`。

```
// 仅检查元素存在
//android.widget.TextView[@resource-id="com.onyx:id/title"]
//android.widget.Button[@resource-id="com.onyx:id/btn_create"]

// 检查元素存在 + 文本对比
//android.widget.TextView[@resource-id="com.onyx.android.note:id/tv_title"],请在录音或转写文字完成后，再使用导出功能
```

- 不写 `,期望文本` → 只验证元素可见
- 写 `,期望文本` → 同时验证 `element.text` 与期望文本一致
- 文本不匹配同样标记为失败

### D 列 — 检查元素

**格式**：每行一个 XPath，可选附加期望文本 。



- 不写  → 只验证元素可见
- 写  → 同时验证  与期望文本一致
- 文本不匹配同样标记为失败


---

## 四、程序是怎么工作的（执行流程）

```
1. 读取 test_cases.xlsx（云端优先 → 缓存 → 本地）
   ├── 解析每一行的"操作步骤" → 拆分出每一步 + 提取【】标签
   ├── 解析"预期结果" → 拆分出每步的预期页面 key
   ├── 解析"前置条件" → 判断哪些用例该跳过
   ├── 按优先级过滤 + 模块筛选
   └── 匹配【】标签 → element_key

2. 读取 elements.xlsx（云端优先 → 缓存 → 本地）
   ├── 把所有元素的 match → key 建立索引
   ├── 加载每个元素的定位信息和动作类型
   └── 加载「预期结果」sheet → 页面 XML 定义

3. 连接设备并执行
   ├── 检查设备连接、语言、WiFi
   ├── 清理笔记应用数据（每次测试前都清干净）
   ├── 逐条执行测试用例
   │   ├── 检查前置条件 → 不符合就跳过
   │   ├── 用例完整性校验 → 步骤号重复/元素缺失 → 跳过
   │   ├── 逐步骤执行
   │   │   ├── 点击 → 找到元素 → 点击
   │   │   ├── 输入 → 找到输入框 → 填入文字
   │   │   ├── 长按 → 找到元素 → 长按
   │   │   ├── 关闭弹窗 → 找到弹窗 → 点击关闭
   │   │   └── Toast检查 → 验证弹出提示
   │   ├── 每步后执行预期结果检查
   │   │   └── XML 对比：预期元素 vs 实际页面 → 缺失则失败
   │   └── 记录通过/失败/跳过
   └── 打印测试汇总

4. 输出结果
   └── 打印"通过=X, 不通过=Y, 跳过=Z" + XML 对比详情
```

---

## 五、项目文件结构（每个文件是干什么的）

```
boox_automation/
│
├── engine/                     ← 🎯 核心：Excel 驱动测试框架
│   ├── parser.py              ← 解析 Excel 中的步骤和前置条件文字
│   ├── elements.py            ← 元素加载 + 预期结果匹配
│   ├── reporter.py            ← 统计通过/失败/跳过数量
│   └── xml_checker.py         ← XML 页面对比引擎
│
├── tests/                      ← 🧪 测试用例和入口
│   ├── test_excel_runner.py   ← 🎯 Excel 用例的 pytest 运行入口（最常用）
│   ├── helpers.py             ← 公共业务方法（创建笔记、导入文件等）
│   ├── test_file_import.py    ← 文件导入测试
│   └── performance/           ← 性能测试
│
├── ui_ops/                     ← ⚙️ UI 操作层
│   ├── operations.py          ← 所有设备操作的底层方法（点击、等待、查找元素）
│   ├── element_catalog.py     ← 元素 ID → 中文描述（报错时用）
│   └── logcat.py              ← 抓取设备日志（性能测试用）
│
├── devices/                    ← 📱 设备管理
│   ├── info.py                ← 获取设备信息（版本、分辨率、内存等）
│   ├── registry.py            ← 设备型号注册表（从 models/ 加载 YAML）
│   └── models/                ← 设备型号库（YAML 格式）
│
├── core/                       ← 🔧 基础设施
│   ├── config.py              ← 配置加载器（读取 config.yaml）
│   ├── feishu.py              ← 飞书 API 客户端（云端加载用例/元素）
│   ├── health.py              ← Appium/ADB 健康检查
│   ├── paths.py               ← 产物路径管理（截图、报告、日志）
│   └── cleanup.py             ← 自动清理旧的测试产物
│
├── scripts/                    ← 🛠 辅助工具
│   ├── extract_page_xml.py    ← 提取设备页面 XML（粘贴到预期结果 C 列）
│   └── run_report.py          ← 生成 Allure 可视化测试报告
│
├── data/                       ← ✏️ 数据文件（你维护的内容）
│   ├── test_cases.xlsx        ← 测试用例（步骤、前置条件）
│   └── elements.xlsx          ← 页面元素定义 + 预期结果 sheet
│
├── driver.py                   ← Appium 驱动连接管理
└── conftest.py                 ← pytest 全局配置（fixture、标记、日志）
```

---

## 六、怎么运行测试

### 方式一：PyCharm 中直接跑（最常用）

在 PyCharm 中打开 `test_excel_runner.py`，右键 → **Run**。

- 会自动连接设备、清理数据、执行 P0 用例
- 结果打印在控制台

### 方式二：命令行跑

```bash
# 跑所有 Excel 用例
pytest boox_automation/tests/test_excel_runner.py -s

# 只跑指定模块
NOTE_TEST_MODULES=笔记首页,手写笔记 pytest boox_automation/tests/test_excel_runner.py -s

# 提取当前页面 XML（用于预期结果 sheet）
python boox_automation/scripts/extract_page_xml.py
```

### 方式三：生成可视化报告

```bash
python boox_automation/scripts/run_report.py
```

会自动打开浏览器展示 Allure 报告（包含截图、步骤耗时等）。

### 运行前提

1. 设备通过 USB 连接电脑，开启 USB 调试
2. Appium 服务已启动（或配置了自动启动）
3. 设备语言为中文（zh-CN）
4. 设备已连接 WiFi

---

## 七、常见问题

**Q: 步骤中写了 `【xxx】` 但程序说"未匹配到任何元素"？**

A: 检查 `elements.xlsx` 中是否有 match 列等于"xxx"的元素。注意：
- match 要和 `【】` 里的文字**完全一致**（包括标点符号）
- 如果文字写错了或者没定义，就会匹配失败
- 匹配失败的步骤会被**跳过**（不会导致用例失败）

**Q: 用例明明失败了，为什么汇总显示"通过"？**

A: 可能的情况：
- 测试还没跑到那一步就中断了（比如应用没启动成功）
- 步骤操作成功但预期结果 K 列为空（会 WARNING 提示缺断言）

**Q: 怎么让一条用例只在平板上跑？**

A: 在前置条件列（I 列）加上 `【平板设备执行】`。程序会自动判断当前设备类型，不符合就跳过。

**Q: 多个用例共用同样的步骤怎么写？**

A: 目前不支持步骤复用，每条用例在 Excel 中独立一行。如果步骤类似，可以复制行再修改。

**Q: 为什么跑完后 Excel 文件没有被修改？**

A: 设计如此。测试结果只打印在控制台日志中，不会写回原始 `test_cases.xlsx`。这样避免多人协作时的文件冲突。

**Q: 程序报错"元素在 3 次重试后仍失败"？**

A: 说明页面上找不到这个元素。常见原因：
- 页面还没加载完 → 程序会等，但有时仍然超时
- 元素在当前页面上不存在（比如还在上一页）
- 定位表达式写错了
- 检查截图（程序会自动截图保存到 `artifacts/screenshots/` 目录）

**Q: 怎么添加新的设备型号？**

A: 在 `devices/models/` 目录下找到对应的 YAML 文件，按格式添加一行。需要知道设备的 platform、region、type、size、colour 属性。
